# Face Recognition Restaurant Access Control System
# Dependencies for facial recognition and GUI

# Core dependencies - install in this order
numpy==1.24.3
Pillow==10.0.1
opencv-python==4.8.1.78

# Face recognition dependencies
# Note: On Windows, dlib-binary is recommended instead of dlib
# If dlib-binary doesn't work, try: pip install cmake then pip install dlib
dlib-binary==19.24.1
face-recognition==1.3.0
face-recognition-models==0.3.0

# GUI dependencies
# Note: tkinter is included with Python standard library

# Additional utilities
Click==8.3.0